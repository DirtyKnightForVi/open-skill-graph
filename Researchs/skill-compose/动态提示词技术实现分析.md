# Skill Compose 动态提示词技术实现深度分析

## 核心问题解答

**动态提示词是通过"硬拼凑"数据库内容实现的，而不是通过其他智能体生成。**

让我详细分析这个技术实现：

## 技术实现机制

### 1. 纯数据库查询 + 字符串拼接（硬拼凑）

从代码分析可以看出，系统采用的是**最直接的实现方式**：

```python
def _build_equipped_skills_section(self) -> str:
    """Build the equipped skills section for system prompt."""
    from app.agent.tools import _fetch_skill_content_from_registry
    from app.core.skill_config import check_skill_env_ready

    if not self.allowed_skills:
        return "No skills equipped. Use `list_skills` to see available skills."

    lines = ["The following skills are equipped and ready to use:\n"]

    for skill_name in self.allowed_skills:
        try:
            registry_skill = _fetch_skill_content_from_registry(skill_name)
            if not registry_skill:
                lines.append(f"### {skill_name}")
                lines.append("(Skill not found)")
                lines.append("")
                continue

            description = registry_skill.get("description", "")
            content_text = registry_skill.get("content", "")

            triggers = []
            # Look for trigger words in content
            content_lower = content_text.lower()
            if "trigger" in content_lower:
                # Extract trigger section
                for line in content_text.split("\n"):
                    if "- \"" in line or "- '" in line or '- "' in line:
                        trigger = line.strip().lstrip("-").strip().strip("'\"")
                        if trigger and len(trigger) < 50:
                            triggers.append(trigger)

            lines.append(f"### {skill_name}")
            if description:
                lines.append(f"**Description:** {description}")
            if triggers:
                lines.append(f"**Triggers:** {', '.join(triggers[:5])}")

            # Check for missing environment variables
            try:
                ready, missing = check_skill_env_ready(skill_name)
                if not ready and missing:
                    lines.append(f"**WARNING:** Missing environment variables: {', '.join(missing)}")
                    lines.append("This skill may not work properly until these are configured.")
            except Exception:
                pass  # Silently ignore if config check fails

            lines.append("")
        except Exception as e:
            lines.append(f"### {skill_name}")
            lines.append(f"(Error loading skill: {e})")
            lines.append("")

    return "\n".join(lines)
```

### 2. 数据库查询实现

```python
def _fetch_skill_content_from_registry(skill_name: str) -> Optional[Dict[str, Any]]:
    """Fetch skill content (SKILL.md) from the registry database (sync)."""
    try:
        with SyncSessionLocal() as session:
            # Get skill
            result = session.execute(
                select(SkillDB).where(SkillDB.name == skill_name)
            )
            skill = result.scalar_one_or_none()
            if not skill or not skill.current_version:
                return None

            # Get version content
            version_result = session.execute(
                select(SkillVersionDB).where(
                    SkillVersionDB.skill_id == skill.id,
                    SkillVersionDB.version == skill.current_version
                )
            )
            version = version_result.scalar_one_or_none()
            if not version:
                return None

            return {
                "name": skill_name,
                "description": skill.description or "",
                "content": version.skill_md or "",
                "version": skill.current_version,
            }
    except Exception as e:
        print(f"Warning: Failed to fetch skill '{skill_name}' from registry: {e}")
        return None
```

## 技术实现分析

### 1. **纯字符串处理**
- **没有使用 LLM 生成**：完全通过 Python 字符串操作
- **没有复杂算法**：简单的循环和条件判断
- **没有外部服务调用**：只依赖本地数据库查询

### 2. **数据库驱动的硬拼凑**
```python
# 典型的"硬拼凑"模式
lines = []
for skill in skills:
    lines.append(f"### {skill_name}")
    if description:
        lines.append(f"**Description:** {description}")
    if triggers:
        lines.append(f"**Triggers:** {', '.join(triggers)}")
    lines.append("")
return "\n".join(lines)
```

### 3. **触发词提取的朴素方法**
```python
# 简单的字符串匹配和提取
triggers = []
content_lower = content_text.lower()
if "trigger" in content_lower:
    for line in content_text.split("\n"):
        if "- \"" in line or "- '" in line or '- "' in line:
            trigger = line.strip().lstrip("-").strip().strip("'\"")
            if trigger and len(trigger) < 50:
                triggers.append(trigger)
```

## 为什么采用"硬拼凑"？

### 1. **简单可靠**
- 避免了 LLM 生成的不确定性
- 减少了系统复杂性
- 提高了构建速度和可靠性

### 2. **确定性输出**
- 相同输入总是产生相同的输出
- 便于调试和问题排查
- 符合工程化的可预测性要求

### 3. **性能考虑**
- 数据库查询 + 字符串拼接非常快速
- 没有网络调用延迟
- 适合高频次的智能体初始化

### 4. **内容控制**
- 完全控制输出格式和内容
- 避免 LLM 可能产生的格式错误
- 确保提示词的一致性和规范性

## 技术架构特点

### 1. **同步数据库查询**
```python
with SyncSessionLocal() as session:
    result = session.execute(select(SkillDB).where(SkillDB.name == skill_name))
    skill = result.scalar_one_or_none()
```
- 使用同步会话避免异步阻塞
- 直接 SQL 查询，没有 ORM 复杂性
- 异常处理完善，单个技能失败不影响整体

### 2. **多层数据获取**
```
SkillDB (基本信息) → SkillVersionDB (完整内容) → 格式化输出
```
- 分层获取，减少不必要的数据传输
- 版本控制支持，确保获取最新内容
- 错误隔离，各层独立处理异常

### 3. **实时状态检查**
```python
ready, missing = check_skill_env_ready(skill_name)
if not ready and missing:
    lines.append(f"**WARNING:** Missing environment variables: {', '.join(missing)}")
```
- 运行时环境验证
- 用户友好的错误提示
- 预防性错误处理

## 与其他方案的对比

### vs. LLM 生成提示词
| 硬拼凑方案 | LLM 生成方案 |
|------------|---------------|
| ✅ 快速可靠 | ❌ 可能产生幻觉 |
| ✅ 确定性输出 | ❌ 输出不稳定 |
| ✅ 简单实现 | ❌ 需要额外 LLM 调用 |
| ✅ 易于调试 | ❌ 难以排查问题 |

### vs. 预定义模板
| 硬拼凑方案 | 模板方案 |
|------------|----------|
| ✅ 完全动态 | ❌ 需要预定义模板 |
| ✅ 实时信息 | ❌ 可能信息过时 |
| ✅ 灵活组合 | ❌ 组合模式固定 |
| ✅ 环境感知 | ❌ 缺乏运行时状态 |

## 技术局限与考量

### 1. **格式限制**
- 固定的 Markdown 格式
- 有限的表达能力
- 无法生成复杂的逻辑结构

### 2. **内容依赖**
- 完全依赖数据库中的 SKILL.md 内容
- 无法智能总结或提炼重点
- 对原始内容质量要求高

### 3. **语言处理简单**
- 简单的字符串匹配提取触发词
- 没有 NLP 处理或语义理解
- 可能遗漏复杂的触发条件

## 结论

Skill Compose 的动态提示词构建采用的是**最朴实但最有效**的技术方案：

### **确实是"硬拼凑"**
- 数据库查询 + 字符串拼接
- 没有使用其他智能体或 LLM
- 纯工程化的实现方式

### **但非常有效**
- 简单可靠，性能优秀
- 确定性输出，便于调试
- 灵活支持任意技能组合

### **体现了实用主义**
- 不追求技术炫技
- 专注于解决实际问题
- 在复杂性和效果之间找到平衡

这种"硬拼凑"方案虽然不够"智能"，但在实际应用中表现出了极高的可靠性和效率，是工程化思维的典型体现。它证明了一个道理：**最适合的技术往往是最简单直接的技术**。